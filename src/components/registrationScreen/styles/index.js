import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  errorMessage: {
    color: "red",
  },
  passwordError: {
    color: "red",
    marginBottom: 5,
  },
});
